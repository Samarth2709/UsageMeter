# Independent review prompts

## Design and feasibility review

Review the planned Claude web usage reader in this checkout. Read plan.md, server.js account identity/discovery/refresh and login/logout paths, electron-main.js refresh/IPC setup, packaging, and applicable review instructions. Propose the smallest safe integration; identify concrete identity, credential, remote-content, stale-data, and lifecycle risks. The intended transport is a sandboxed Electron BrowserWindow with a dedicated persistent session, observing responses generated by normal navigation and usage-page Refresh clicks. No copied Chrome cookies or private API replay. Do not modify files or start external actions. Return concise actionable feedback grounded in code.

## Final correctness and security review

Review the implementation diff against origin/main and plan.md. Examine account binding, polling single-flight and timeouts, 429/auth handling, honest cached timestamps, login/logout/delete races, remote-content privilege isolation, packaging, and test coverage. Run relevant local tests if useful. Do not modify files or perform live external actions. Report only actionable findings with priority and exact file/line; explicitly state if none remain. Distinguish verified behavior from assumptions.

## Follow-up review of lifecycle fixes

Recheck the three implementation-review findings: backoff after cancellation, page-level 429, and concurrent reads while browser storage is cleared. Inspect the new regression tests, account-binding checks for pending/login results, and one coalesced follow-up refresh after login or manual refresh. The reader uses one fresh page document per poll and a per-row persistent partition. Review the current complete diff again for remaining actionable issues. Stay read-only; do not make external requests or change files.

## Chrome replacement review

Review the replacement of the embedded browser with native Google Chrome Apple Events in claude-web-usage.js and its Electron integration. Read plan.md and the complete diff from ddb29c3. Check account/profile isolation, same-origin request safety, timeouts and rate limits, concurrent sign-in/read/disconnect races, honest cached data, tab persistence and packaging permission requirements. The user explicitly wants Sign In in existing Chrome. Stay read-only, do not perform live external actions, and report only actionable findings with file and line.

## Chrome packaging review

Review the approved Chrome permission changes to package.json, package-lock.json, scripts/entitlements.mac.plist, scripts/dist-mac.sh, and the shell compatibility/release path. Ensure existing runtime entitlements are preserved, app usage text is correct, and the new Chrome core cannot activate on the old 0.2.12 shell. Read only; no external actions. Report concrete issues with source lines. Root is building and verifying the installed Chrome path concurrently.

## Live Chrome recovery regression review

Live installed Sign In opened Chrome but selected the current personal profile with disabled JavaScript. The permission error lacked the exact Sign in to Claude phrase needed by the existing renderer. Review the corrected actionable error strings and association removal on a permission failure, plus their focused tests. The work profile already has JavaScript access enabled. Check that switching to it can recover without changing shared credentials or broadening Chrome permissions. Stay read-only.

## Repeated polling regression review

Live first poll succeeded but the next timed out. Chrome page asset inventory then contained no bootstrap/usage resource entries: Claude clears performance resource timings. The collector now retains the already-observed bootstrap URL in that page document, and Sign In reloads an existing dedicated tab to recover an expired page context. Review this narrow fix, regression tests and updated Chromium fixture which clears resource timings between changing readings. Stay read-only and do not make external requests.

## Saved organization polling review

The live reconnect can begin after resource timings have already been cleared, so retaining a URL from the first read is insufficient. The collector now uses the saved, UUID-validated organization to form the same-origin bootstrap URL; new rows without an organization still discover it from the page. Identity is checked before usage and again afterward. Review this removal of the timing dependency and its first-read-with-empty-resource-buffer regression. Pending phases only improve timeout errors. Read-only, no external calls.
